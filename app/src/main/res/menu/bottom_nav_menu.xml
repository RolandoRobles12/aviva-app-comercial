<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Inicio / Dashboard -->
    <item
        android:id="@+id/navigation_home"
        android:icon="@drawable/ic_home_24"
        android:title="Inicio" />

    <!-- Métricas y Reportería -->
    <item
        android:id="@+id/navigation_metrics"
        android:icon="@drawable/ic_metrics_24"
        android:title="Métricas" />

    <!-- Asistencia -->
    <item
        android:id="@+id/navigation_attendance"
        android:icon="@drawable/ic_attendance_24"
        android:title="Asistencia" />

    <!-- Ligas / Competencias -->
    <item
        android:id="@+id/navigation_leagues"
        android:icon="@drawable/ic_leagues_24"
        android:title="Ligas" />

    <!-- Perfil (incluye Insignias y Plan de Carrera) -->
    <item
        android:id="@+id/navigation_profile"
        android:icon="@drawable/ic_profile_24"
        android:title="Perfil" />

    <!-- Admin (solo visible para SUPER_ADMIN y ADMIN) -->
    <item
        android:id="@+id/navigation_admin"
        android:icon="@drawable/ic_admin_24"
        android:title="Admin"
        android:visible="false" />

</menu>
